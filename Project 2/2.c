/*
#include <stdio.h>
#include<math.h>

int main()
{
	int i,size;
    printf("Enter Size: ");
    scanf("%d",&size);
    int a[size];

    for(i=0;i<size;i++)
       scanf("%d",&a[i]);
    for(i=0;i<size;i++)
       printf("%d  ",a[i]);
	
	return 0;  
}
*/
#include<stdio.h>
#include<math.h>
int main()
{
    printf("%d\n",pow(2,5));
    return 0;
}